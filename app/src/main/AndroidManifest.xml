<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.com.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.NewActivity"
            android:label="@string/new_Activity_label">

        </activity>
        <activity
            android:name=".activity.ThirdActivity"
            android:label="Third Activity" />

        <service android:name=".service.FirstService" />

        <service android:name=".service.BoundedService" />

        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.BoundedActivity"
            android:label="@string/bounded_activity" />


    </application>

</manifest>
